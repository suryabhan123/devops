---
  tasks:
    - name: create the cron for list the files in /tmp
      copy:
        src: files/list.sh
        dest: /tmp/

      cron:
        name: list the files in tmp directory
        job: sh /tmp/list.sh

        month: *
        day: 30
        hour: 17
        minute: 05
        weekday: *

---
  - hosts: all
    become: yes
    gather_facts: false

    tasks:
    - name: copy the directory
      copy:
        src: /tmp/etc
        dest: /tmp/abc
        directory_mode: yes
        remote_src: yes

---
- hosts: all
  tasks:
  - name: copy file
    copy:
     src: /etc/passwd
     dest: /tmp/passwd
     remote_src: yes

  # - name: Replace example
  #   replace:
  #    path: /tmp/passwd
  #    regexp: 'timed'
  #    replace: "shririta"

  - name: replace the word
    replace:
      path: /tmp/passwd
      regexp: 'test1'
      replace: "tata"
